int main(){
int choice;
cin>>choice;
if(choice==1) statements;
else if(choice==2||choice==4) statement;
else if(choice==3||choice==5) statement;
else{
cout<<"choice must be1 through 5"<<endl;
return 1;
 }
}
-----------------------------------------------------------------------------------

Switch(choice){
case 1;
statement;
break;
case 2:
case 4:
statement;
//MISSING THE break INTENTIONALLY PLAEASE MARK:FALL THROUGH TO NEXT CASE!!
case 3:
case 5:
statement；
break;
default:{
cout<<"choice must be1 through 5"<<endl;
return 1;
}

//switch maybe faster than if-else, if there are lots of branches in the program.
/*switch only can work with condition only contains the constant;
neither the string nor the double can be applied*/
-----------------------------------------------------------------------------------


cout<<"How many times do you want to begreeted?";
int nTimes;
cin>>nTimes;
int n=0;
while(n<nTimes){
cout<<"Hello"<<endl;
n=n+1;
}

////////////////////////////////////////////////////////////////////////////////////

int n=1;
do{
cout<<"Hello"<<endl;
n++}whiel(n<=nTime);

////////////////////////////////////////////////////////////////////////////////////

for(initialization; stay-in-loop-condition; prepare-for-next-iteration)
statement;

-----------------------------------------------------------------------------------
